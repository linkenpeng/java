server:
  port: 8082
spring:
  application:
    name: cache
  redis:
    host: localhost
    port: 6379
    database: 0
    redisson:
      database2: true
    password:
    timeout: 10000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
logging:
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} %level %msg%n'